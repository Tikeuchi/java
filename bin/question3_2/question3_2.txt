question3_2

次のようなHeroクラスがあります。

public class Hero {
	private String name;
	public Hero(String name) { this.name = name; }
	public String getName() { return this.name; }
}

勇者2名（斎藤、鈴木）をHeroとしてインスタンス化してArrayListに格納し、
1つずつ順番に取り出して名前を表示するプログラムを作成してください。



■解説

・Heroクラスを使って勇者2名のインスタンスを用意します
・ArrayListに格納するにはaddメソッドを使用します

※必要なパッケージはインポート済みです